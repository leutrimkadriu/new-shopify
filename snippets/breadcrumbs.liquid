{% unless template.name == 'index' %}<nav class="nx-breadcrumbs nx-page-width" aria-label="Breadcrumb"><ol><li><a href="{{ routes.root_url }}">{{ 'general.breadcrumbs.home' | t }}</a></li><li aria-hidden="true">/</li>{% if collection %}<li><a href="{{ collection.url }}">{{ collection.title | escape }}</a></li>{% if product %}<li aria-hidden="true">/</li>{% endif %}{% endif %}<li aria-current="page">{{ page_title | escape }}</li></ol></nav>{% endunless %}
