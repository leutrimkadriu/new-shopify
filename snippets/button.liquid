{% if label != blank and url != blank %}
  <a class="nx-button{% if style %} nx-button--{{ style }}{% endif %}" href="{{ url }}">{{ label | escape }}{% if show_arrow %}{% render 'icon', icon: 'arrow' %}{% endif %}</a>
{% endif %}
