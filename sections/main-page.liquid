{% render 'breadcrumbs' %}
<article class="nx-section nx-page-width">
  <header>
    Trimi
    <h1>{{ page.title | escape }}</h1>
  </header>
  <div class="nx-rte">{{ page.content }}</div>
</article>
{% schema %}{"name":"Page","settings":[]}{% endschema %}
