(() => { class NxMediaGallery extends HTMLElement { connectedCallback(){ this.querySelectorAll('[data-thumbnail]').forEach(button=>button.addEventListener('click',()=>{ const media=this.querySelector(`[data-media-id="${button.dataset.thumbnail}"]`); media?.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'nearest'}); })); } } customElements.define('nx-media-gallery',NxMediaGallery); })();
