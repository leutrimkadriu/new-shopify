(() => { class NxPredictiveSearch extends HTMLElement { connectedCallback(){ this.input=this.querySelector('input'); this.results=this.querySelector('[data-results]'); let timer; this.input?.addEventListener('input',()=>{ clearTimeout(timer); timer=setTimeout(()=>this.search(),250); }); } async search(){ const query=this.input.value.trim(); if(query.length<2){this.results.innerHTML='';return;} this.setAttribute('aria-busy','true'); try{ const url=`${window.NX.routes.predictive}?q=${encodeURIComponent(query)}&resources[type]=product,collection,page,article&resources[limit]=6&section_id=predictive-search`; this.results.innerHTML=await fetch(url).then(r=>r.text()); }catch(error){ window.NXTheme.live(error.message); }finally{ this.removeAttribute('aria-busy'); } } } customElements.define('nx-predictive-search',NxPredictiveSearch); })();
